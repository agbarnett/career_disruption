Versions of the data without the respondents' comments in RData and CSV format.

The RData includes:
- `data` the survey response data with 246 observations
- `labels` a data frame that contains descriptive labels for each variable 
- `nhmrc` recent data from the NHMRC on success rates
- `ordering` how the results are ordered in plots
